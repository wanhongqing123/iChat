# iChat 登录页 · iOS 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `UI/iOS/` 落地 iChat 第一期登录页：手机号 → 6 位验证码 → mock 校验 → 进入空白主页占位。

**Architecture:** Xcode iOS App 工程，SwiftUI 声明式 UI，`NavigationStack` 承载两屏。`LoginStore: ObservableObject` 持有 `MockAuthService`，60s 倒计时用 Swift Concurrency `Task`。无第三方依赖（YAGNI）。

**Tech Stack:** Swift 5.10+ · **iOS 17+** · Xcode 26+ · SwiftUI · NavigationStack · async/await · XCTest · XCUITest · **xcodegen**（脚手架生成）

**Spec:** `docs/superpowers/specs/2026-05-07-login-screen-design.md`

**Working dir:** `UI/iOS/`（从空目录开始）

---

## 文件结构

```
UI/iOS/
├── iChatLogin.xcodeproj/             # Xcode 自动生成
├── iChatLogin/
│   ├── iChatLoginApp.swift
│   ├── ContentView.swift             # 顶层入口（Login or Home）
│   ├── Theme/
│   │   ├── Colors.swift
│   │   ├── Typography.swift
│   │   ├── Shapes.swift
│   │   └── Theme.swift               # ViewModifier 集合
│   ├── Data/
│   │   ├── PhoneUtils.swift          # format / mask / isValid
│   │   └── MockAuthService.swift
│   ├── Login/
│   │   ├── LoginStore.swift          # ObservableObject 状态机
│   │   ├── PhoneView.swift
│   │   ├── CodeView.swift
│   │   └── Components/
│   │       ├── PrimaryButton.swift
│   │       ├── PhoneField.swift
│   │       └── CodeBoxes.swift
│   ├── Home/
│   │   └── HomePlaceholderView.swift
│   ├── Assets.xcassets/              # Xcode 默认
│   └── Info.plist                    # Xcode 默认 + 锁竖屏
├── iChatLoginTests/
│   ├── PhoneUtilsTests.swift
│   ├── MockAuthServiceTests.swift
│   └── LoginStoreTests.swift
└── iChatLoginUITests/
    └── LoginE2ETests.swift
```

---

## Task 1: Scaffold Xcode 工程（xcodegen）

为了 CLI / subagent 友好，不走 Xcode UI 创建工程的方式。改为用 xcodegen
通过一份 `project.yml` 描述工程结构后生成 `.xcodeproj`。等价于 Xcode 模板
"iOS App + SwiftUI + Include Tests"，但所有内容都是文本可审/可重生。

前置：`brew install xcodegen` （已装）。

**Files:**
- Create: `UI/iOS/project.yml`
- Create: `UI/iOS/iChatLogin/iChatLoginApp.swift`
- Create: `UI/iOS/iChatLogin/ContentView.swift`
- Create: `UI/iOS/iChatLogin/Info.plist`
- Create: `UI/iOS/iChatLoginTests/iChatLoginTests.swift`（占位）
- Create: `UI/iOS/iChatLoginUITests/iChatLoginUITests.swift`（占位）
- 由 xcodegen 生成（不入 git，加 .gitignore）：`UI/iOS/iChatLogin.xcodeproj`

- [ ] **Step 1: 创建 `project.yml`**

```yaml
name: iChatLogin
options:
  bundleIdPrefix: com.ichat
  deploymentTarget:
    iOS: "17.0"
  developmentLanguage: zh-Hans
settings:
  base:
    SWIFT_VERSION: "5.10"
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    CODE_SIGN_STYLE: Automatic
    CODE_SIGNING_REQUIRED: "NO"
    CODE_SIGN_IDENTITY: ""
targets:
  iChatLogin:
    type: application
    platform: iOS
    sources:
      - iChatLogin
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ichat.iChatLogin
        INFOPLIST_FILE: iChatLogin/Info.plist
        ENABLE_PREVIEWS: "YES"
        TARGETED_DEVICE_FAMILY: "1"     # iPhone only (no iPad)
  iChatLoginTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - iChatLoginTests
    dependencies:
      - target: iChatLogin
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ichat.iChatLoginTests
  iChatLoginUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - iChatLoginUITests
    dependencies:
      - target: iChatLogin
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ichat.iChatLoginUITests
schemes:
  iChatLogin:
    build:
      targets:
        iChatLogin: all
        iChatLoginTests: [test]
        iChatLoginUITests: [test]
    test:
      targets:
        - iChatLoginTests
        - iChatLoginUITests
```

- [ ] **Step 2: 创建源文件骨架**

`iChatLogin/iChatLoginApp.swift`：
```swift
import SwiftUI

@main
struct iChatLoginApp: App {
    var body: some Scene {
        WindowGroup { ContentView() }
    }
}
```

`iChatLogin/ContentView.swift`：
```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello iChat")
            .padding()
    }
}
```

`iChatLogin/Info.plist`（精简版，仅锁定竖屏 + 必要键）：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>iChat</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict/>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>
    <key>UIRequiresFullScreen</key>
    <true/>
</dict>
</plist>
```

`iChatLoginTests/iChatLoginTests.swift`（占位，避免空 target）：
```swift
import XCTest
@testable import iChatLogin

final class iChatLoginTests: XCTestCase {
    func test_truth() { XCTAssertTrue(true) }
}
```

`iChatLoginUITests/iChatLoginUITests.swift`（占位）：
```swift
import XCTest

final class iChatLoginUITests: XCTestCase {
    func test_app_launches() {
        let app = XCUIApplication()
        app.launch()
        // 仅验证启动不崩；具体 E2E 在 Task 12
    }
}
```

- [ ] **Step 3: 生成 Xcode 工程**

```bash
cd /Users/hongqingwan/OpenSource/iChat/UI/iOS
xcodegen generate
```

Expected：生成 `iChatLogin.xcodeproj` 目录。

- [ ] **Step 4: 编辑 `.gitignore` 让生成的工程文件不入库**

在仓库根 `.gitignore` 末尾追加：

```
# iOS / xcodegen-generated
UI/iOS/iChatLogin.xcodeproj/
UI/iOS/*.xcworkspace/
UI/iOS/.build/
UI/iOS/DerivedData/
```

- [ ] **Step 5: 验证脚手架可编译**

```bash
cd /Users/hongqingwan/OpenSource/iChat/UI/iOS
xcodebuild -scheme iChatLogin -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -20
```

Expected：`** BUILD SUCCEEDED **`。

- [ ] **Step 6: 提交**

```bash
git -C /Users/hongqingwan/OpenSource/iChat add .gitignore UI/iOS
git -C /Users/hongqingwan/OpenSource/iChat commit -m "feat(ios): scaffold SwiftUI project (xcodegen, iOS 17+, portrait)"
```

---

## Task 2: 设计 token 主题

**Files:**
- Create: `UI/iOS/iChatLogin/Theme/Colors.swift`
- Create: `UI/iOS/iChatLogin/Theme/Typography.swift`
- Create: `UI/iOS/iChatLogin/Theme/Shapes.swift`
- Create: `UI/iOS/iChatLogin/Theme/Theme.swift`

- [ ] **Step 1: 创建 `Colors.swift`**

```swift
import SwiftUI

extension Color {
    static let brandPrimary         = Color(red: 0x4F/255.0, green: 0x86/255.0, blue: 0xFF/255.0)
    static let brandPrimaryPressed  = Color(red: 0x3B/255.0, green: 0x6B/255.0, blue: 0xE0/255.0)
    static let brandPrimaryDisabled = Color(red: 0xB7/255.0, green: 0xCD/255.0, blue: 0xFA/255.0)

    static let bgPage    = Color(red: 0xF2/255.0, green: 0xF6/255.0, blue: 0xFF/255.0)
    static let bgSurface = Color.white

    static let borderInput      = Color(red: 0xDC/255.0, green: 0xE6/255.0, blue: 0xFA/255.0)
    static let borderInputFocus = Color.brandPrimary

    static let textPrimary   = Color(red: 0x1A/255.0, green: 0x22/255.0, blue: 0x33/255.0)
    static let textSecondary = Color(red: 0x78/255.0, green: 0x84/255.0, blue: 0xA3/255.0)
    static let textTertiary  = Color(red: 0x9A/255.0, green: 0xA3/255.0, blue: 0xB5/255.0)

    static let stateError = Color(red: 0xE5/255.0, green: 0x48/255.0, blue: 0x4D/255.0)
}
```

- [ ] **Step 2: 创建 `Typography.swift`**

```swift
import SwiftUI

extension Font {
    static let titleLg = Font.system(size: 20, weight: .semibold)
    static let bodyMd  = Font.system(size: 14, weight: .regular)
    static let bodySm  = Font.system(size: 13, weight: .regular)
    static let caption = Font.system(size: 12, weight: .regular)
    static let codeBox = Font.system(size: 20, weight: .semibold)
}
```

- [ ] **Step 3: 创建 `Shapes.swift`**

```swift
import SwiftUI

enum Shapes {
    static let inputCornerRadius:   CGFloat = 14
    static let buttonCornerRadius:  CGFloat = 24   // pill
    static let codeBoxCornerRadius: CGFloat = 12
}
```

- [ ] **Step 4: 创建 `Theme.swift`**（占位，便于未来扩展 modifier）

```swift
import SwiftUI

extension View {
    /// 统一页面背景色 + 安全区
    func iChatPage() -> some View {
        self
            .background(Color.bgPage.ignoresSafeArea())
    }
}
```

- [ ] **Step 5: 在 `ContentView.swift` 应用主题验证**

```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            Color.bgPage.ignoresSafeArea()
            Text("iChat").font(.titleLg).foregroundStyle(Color.textPrimary)
        }
    }
}
```

- [ ] **Step 6: 在 `iChatLoginApp.swift` 入口装载（Xcode 默认已生成）**

```swift
import SwiftUI

@main
struct iChatLoginApp: App {
    var body: some Scene {
        WindowGroup { ContentView() }
    }
}
```

- [ ] **Step 7: 验证**

```bash
xcodebuild -scheme iChatLogin -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build | tail -10
```

Expected：BUILD SUCCEEDED。模拟器肉眼检查：背景 `#F2F6FF`，居中 "iChat"。

- [ ] **Step 8: 提交**

```bash
git add UI/iOS/iChatLogin/Theme UI/iOS/iChatLogin/ContentView.swift
git commit -m "feat(ios): add design tokens (color/type/shape/theme)"
```

---

## Task 3: 手机号工具函数（TDD）

**Files:**
- Create: `UI/iOS/iChatLoginTests/PhoneUtilsTests.swift`
- Create: `UI/iOS/iChatLogin/Data/PhoneUtils.swift`

- [ ] **Step 1: 写失败测试 `PhoneUtilsTests.swift`**

```swift
import XCTest
@testable import iChatLogin

final class PhoneUtilsTests: XCTestCase {

    func test_digitsOnly_removesNonDigits() {
        XCTAssertEqual(PhoneUtils.digitsOnly("138 0013 8000"), "13800138000")
        XCTAssertEqual(PhoneUtils.digitsOnly("138-0013-8000"), "13800138000")
        XCTAssertEqual(PhoneUtils.digitsOnly("abc"),           "")
    }

    func test_format_appliesGroupedSpacing() {
        XCTAssertEqual(PhoneUtils.format(""),              "")
        XCTAssertEqual(PhoneUtils.format("1"),             "1")
        XCTAssertEqual(PhoneUtils.format("138"),           "138")
        XCTAssertEqual(PhoneUtils.format("1380"),          "138 0")
        XCTAssertEqual(PhoneUtils.format("1380013"),       "138 0013")
        XCTAssertEqual(PhoneUtils.format("13800138"),      "138 0013 8")
        XCTAssertEqual(PhoneUtils.format("13800138000"),   "138 0013 8000")
    }

    func test_format_truncatesAt11Digits() {
        XCTAssertEqual(PhoneUtils.format("13800138000999"), "138 0013 8000")
    }

    func test_isValid_acceptsValidChineseMobile() {
        XCTAssertTrue(PhoneUtils.isValid("13800138000"))
        XCTAssertTrue(PhoneUtils.isValid("15912345678"))
        XCTAssertTrue(PhoneUtils.isValid("18800001111"))
    }

    func test_isValid_rejectsInvalid() {
        XCTAssertFalse(PhoneUtils.isValid(""))
        XCTAssertFalse(PhoneUtils.isValid("1380013800"))      // < 11
        XCTAssertFalse(PhoneUtils.isValid("138001380001"))    // > 11
        XCTAssertFalse(PhoneUtils.isValid("00000000000"))     // 不以 1 开头
        XCTAssertFalse(PhoneUtils.isValid("12345678901"))     // 第二位不在 3..9
        XCTAssertFalse(PhoneUtils.isValid("1a800138000"))     // 含字母
    }

    func test_mask_hidesMiddleFour() {
        XCTAssertEqual(PhoneUtils.mask("13800138000"), "138****8000")
    }

    func test_mask_returnsAsIs_whenNot11Digits() {
        XCTAssertEqual(PhoneUtils.mask(""),      "")
        XCTAssertEqual(PhoneUtils.mask("12345"), "12345")
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
xcodebuild test -scheme iChatLogin -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:iChatLoginTests/PhoneUtilsTests | tail -20
```

Expected：编译失败，`PhoneUtils` 未定义。

- [ ] **Step 3: 实现 `PhoneUtils.swift`**

```swift
import Foundation

enum PhoneUtils {

    static func digitsOnly(_ input: String) -> String {
        input.filter(\.isWholeNumber)
    }

    static func format(_ raw: String) -> String {
        let digits = String(digitsOnly(raw).prefix(11))
        switch digits.count {
        case 0...3:  return digits
        case 4...7:  return "\(digits.prefix(3)) \(digits.dropFirst(3))"
        default:
            let p1 = digits.prefix(3)
            let p2 = digits.dropFirst(3).prefix(4)
            let p3 = digits.dropFirst(7)
            return "\(p1) \(p2) \(p3)"
        }
    }

    static func isValid(_ phone: String) -> Bool {
        let d = digitsOnly(phone)
        guard d.count == 11 else { return false }
        guard d.first == "1" else { return false }
        let second = d[d.index(d.startIndex, offsetBy: 1)]
        return ("3"..."9").contains(second)
    }

    static func mask(_ phone: String) -> String {
        let d = digitsOnly(phone)
        guard d.count == 11 else { return d }
        return "\(d.prefix(3))****\(d.suffix(4))"
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
xcodebuild test -scheme iChatLogin -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:iChatLoginTests/PhoneUtilsTests | tail -20
```

Expected：7 tests passed。

- [ ] **Step 5: 提交**

```bash
git add UI/iOS/iChatLogin/Data/PhoneUtils.swift UI/iOS/iChatLoginTests/PhoneUtilsTests.swift
git commit -m "feat(ios): add PhoneUtils (format/isValid/mask) with tests"
```

---

## Task 4: MockAuthService（TDD）

**Files:**
- Create: `UI/iOS/iChatLoginTests/MockAuthServiceTests.swift`
- Create: `UI/iOS/iChatLogin/Data/MockAuthService.swift`

- [ ] **Step 1: 写失败测试**

```swift
import XCTest
@testable import iChatLogin

final class MockAuthServiceTests: XCTestCase {

    func test_requestCode_returnsSuccess() async throws {
        let svc = MockAuthService()
        let result = try await svc.requestCode(phone: "13800138000")
        XCTAssertEqual(result, .requestSuccess)
    }

    func test_verifyCode_correctCode_returnsVerifySuccess() async throws {
        let svc = MockAuthService()
        let result = try await svc.verifyCode(phone: "13800138000", code: "123456")
        guard case let .verifySuccess(token, isNewUser) = result else {
            XCTFail("expected verifySuccess, got \(result)"); return
        }
        XCTAssertEqual(token, "mock-token")
        XCTAssertEqual(isNewUser, false)
    }

    func test_verifyCode_wrongCode_returnsInvalidCode() async throws {
        let svc = MockAuthService()
        let result = try await svc.verifyCode(phone: "13800138000", code: "000000")
        XCTAssertEqual(result, .invalidCode)
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Expected：编译失败（`MockAuthService` / `AuthResult` 未定义）。

- [ ] **Step 3: 实现 `MockAuthService.swift`**

```swift
import Foundation

enum AuthResult: Equatable {
    case requestSuccess
    case verifySuccess(token: String, isNewUser: Bool)
    case invalidCode
    case networkError
}

class MockAuthService {

    /// 800ms 延迟后返回成功；子类可 override 以注入失败
    func requestCode(phone: String) async throws -> AuthResult {
        try await Task.sleep(nanoseconds: 800_000_000)
        return .requestSuccess
    }

    /// 600ms 延迟后判断 code == "123456"
    func verifyCode(phone: String, code: String) async throws -> AuthResult {
        try await Task.sleep(nanoseconds: 600_000_000)
        return code == "123456"
            ? .verifySuccess(token: "mock-token", isNewUser: false)
            : .invalidCode
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Expected：3 tests passed。

- [ ] **Step 5: 提交**

```bash
git add UI/iOS/iChatLogin/Data/MockAuthService.swift UI/iOS/iChatLoginTests/MockAuthServiceTests.swift
git commit -m "feat(ios): add MockAuthService with tests"
```

---

## Task 5: LoginStore 状态机（TDD）

**Files:**
- Create: `UI/iOS/iChatLoginTests/LoginStoreTests.swift`
- Create: `UI/iOS/iChatLogin/Login/LoginStore.swift`

覆盖 spec §6.1 测试用例 #1, #2, #3, #4, #5, #6, #7, #8。

- [ ] **Step 1: 写失败测试**

```swift
import XCTest
@testable import iChatLogin

@MainActor
final class LoginStoreTests: XCTestCase {

    /// 用一个可注入返回值的 stub 服务
    final class StubService: MockAuthService {
        var requestImpl: (String) async -> AuthResult = { _ in .requestSuccess }
        var verifyImpl:  (String, String) async -> AuthResult = { _, c in
            c == "123456" ? .verifySuccess(token: "t", isNewUser: false) : .invalidCode
        }
        var requestCount = 0
        override func requestCode(phone: String) async throws -> AuthResult {
            requestCount += 1
            return await requestImpl(phone)
        }
        override func verifyCode(phone: String, code: String) async throws -> AuthResult {
            await verifyImpl(phone, code)
        }
    }

    func test_phoneTooShort_canRequestCode_isFalse() {
        let store = LoginStore(service: StubService())
        store.onPhoneChange("1380013")
        XCTAssertFalse(store.canRequestCode)
    }

    func test_phoneInvalidFormat_canRequestCode_isFalse() {
        let store = LoginStore(service: StubService())
        store.onPhoneChange("00000000000")
        XCTAssertFalse(store.canRequestCode)
    }

    func test_phoneValid_canRequestCode_isTrue() {
        let store = LoginStore(service: StubService())
        store.onPhoneChange("13800138000")
        XCTAssertTrue(store.canRequestCode)
    }

    func test_requestCode_movesToCodePhase_andStartsCountdown() async {
        let store = LoginStore(service: StubService(), countdownTickInterval: .milliseconds(10))
        store.onPhoneChange("13800138000")
        await store.requestCode()
        XCTAssertEqual(store.phase, .code)
        XCTAssertEqual(store.countdown, 60)
    }

    func test_countdown_decrementsOverTime() async {
        let store = LoginStore(service: StubService(), countdownTickInterval: .milliseconds(10))
        store.onPhoneChange("13800138000")
        await store.requestCode()
        // 加速：tick=10ms，等 35ms 应该至少递减 3 次
        try? await Task.sleep(nanoseconds: 35_000_000)
        XCTAssertLessThanOrEqual(store.countdown, 57)
        XCTAssertGreaterThanOrEqual(store.countdown, 56)
    }

    func test_verifyCode_correct_emitsLoginSuccess() async {
        let store = LoginStore(service: StubService(), countdownTickInterval: .milliseconds(10))
        store.onPhoneChange("13800138000")
        await store.requestCode()
        await store.onCodeChange("123456")
        XCTAssertTrue(store.didLoginSuccess)
    }

    func test_verifyCode_wrong_setsErrorThenClears() async {
        let store = LoginStore(
            service: StubService(),
            countdownTickInterval: .milliseconds(10),
            errorClearDelay: .milliseconds(50)
        )
        store.onPhoneChange("13800138000")
        await store.requestCode()
        await store.onCodeChange("000000")
        XCTAssertEqual(store.errorMessage, "验证码错误，请重新输入")
        try? await Task.sleep(nanoseconds: 80_000_000)
        XCTAssertNil(store.errorMessage)
        XCTAssertEqual(store.code, "")
    }

    func test_goBack_preservesPhoneAndCountdown() async {
        let store = LoginStore(service: StubService(), countdownTickInterval: .milliseconds(10))
        store.onPhoneChange("13800138000")
        await store.requestCode()
        try? await Task.sleep(nanoseconds: 35_000_000)
        let savedCountdown = store.countdown
        store.goBack()
        XCTAssertEqual(store.phase, .phone)
        XCTAssertEqual(store.phone, "13800138000")
        XCTAssertEqual(store.countdown, savedCountdown)  // 不重置
    }

    func test_requestCode_duringCountdown_skipsRefetchAndReentersCode() async {
        let stub = StubService()
        let store = LoginStore(service: stub, countdownTickInterval: .milliseconds(10))
        store.onPhoneChange("13800138000")
        await store.requestCode()
        XCTAssertEqual(stub.requestCount, 1)
        store.goBack()
        // 用例 #7：倒计时仍在，再次点击 → 切回 Code 屏不重新发码
        await store.requestCode()
        XCTAssertEqual(store.phase, .code)
        XCTAssertEqual(stub.requestCount, 1)
    }

    func test_networkError_onRequestCode_keepsPhonePhase() async {
        let stub = StubService()
        stub.requestImpl = { _ in .networkError }
        let store = LoginStore(service: stub, countdownTickInterval: .milliseconds(10))
        store.onPhoneChange("13800138000")
        await store.requestCode()
        XCTAssertEqual(store.phase, .phone)
        XCTAssertEqual(store.errorMessage, "网络异常，请检查后重试")
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Expected：编译失败（`LoginStore` 等未定义）。

- [ ] **Step 3: 实现 `LoginStore.swift`**

```swift
import Foundation
import SwiftUI

enum LoginPhase { case phone, code }

@MainActor
final class LoginStore: ObservableObject {

    @Published private(set) var phase: LoginPhase = .phone
    @Published var phone: String = ""
    @Published var code: String = ""
    @Published private(set) var countdown: Int = 0
    @Published private(set) var isSubmitting: Bool = false
    @Published private(set) var errorMessage: String?
    @Published private(set) var didLoginSuccess: Bool = false

    private let service: MockAuthService
    private let countdownTickInterval: Duration
    private let errorClearDelay: Duration
    private var countdownTask: Task<Void, Never>?
    private var errorClearTask: Task<Void, Never>?

    init(
        service: MockAuthService = MockAuthService(),
        countdownTickInterval: Duration = .seconds(1),
        errorClearDelay: Duration = .milliseconds(1_200)
    ) {
        self.service = service
        self.countdownTickInterval = countdownTickInterval
        self.errorClearDelay = errorClearDelay
    }

    var canRequestCode: Bool {
        PhoneUtils.isValid(phone) && !isSubmitting
    }

    func onPhoneChange(_ raw: String) {
        phone = String(PhoneUtils.digitsOnly(raw).prefix(11))
    }

    func onCodeChange(_ raw: String) async {
        let digits = String(raw.filter(\.isWholeNumber).prefix(6))
        code = digits
        errorMessage = nil
        if digits.count == 6 { await verifyCode() }
    }

    func requestCode() async {
        guard PhoneUtils.isValid(phone), !isSubmitting else { return }
        // 用例 #7：倒计时未归零 → 直接切回 Code 屏，不重新发码
        if countdown > 0 {
            phase = .code
            errorMessage = nil
            return
        }
        isSubmitting = true; errorMessage = nil
        do {
            switch try await service.requestCode(phone: phone) {
            case .requestSuccess:
                code = ""; phase = .code; countdown = 60; isSubmitting = false
                startCountdown()
            default:
                isSubmitting = false; errorMessage = "网络异常，请检查后重试"
            }
        } catch {
            isSubmitting = false; errorMessage = "网络异常，请检查后重试"
        }
    }

    func resendCode() async {
        guard countdown == 0 else { return }
        do {
            if case .requestSuccess = try await service.requestCode(phone: phone) {
                countdown = 60
                startCountdown()
            } else {
                errorMessage = "网络异常，请检查后重试"
            }
        } catch {
            errorMessage = "网络异常，请检查后重试"
        }
    }

    private func startCountdown() {
        countdownTask?.cancel()
        countdownTask = Task { [weak self] in
            guard let self else { return }
            while await self.countdown > 0 {
                try? await Task.sleep(for: self.countdownTickInterval)
                await self.tick()
            }
        }
    }

    private func tick() {
        countdown = max(0, countdown - 1)
    }

    private func verifyCode() async {
        isSubmitting = true
        defer { isSubmitting = false }
        do {
            switch try await service.verifyCode(phone: phone, code: code) {
            case .verifySuccess: didLoginSuccess = true
            case .invalidCode:   showCodeError()
            case .networkError:  errorMessage = "网络异常，请检查后重试"
            default: break
            }
        } catch {
            errorMessage = "网络异常，请检查后重试"
        }
    }

    private func showCodeError() {
        errorMessage = "验证码错误，请重新输入"
        errorClearTask?.cancel()
        errorClearTask = Task { [weak self] in
            try? await Task.sleep(for: self?.errorClearDelay ?? .milliseconds(1_200))
            await self?.clearError()
        }
    }

    private func clearError() {
        code = ""
        errorMessage = nil
    }

    func goBack() {
        phase = .phone
        errorMessage = nil
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Expected：10 tests passed。

- [ ] **Step 5: 提交**

```bash
git add UI/iOS/iChatLogin/Login/LoginStore.swift UI/iOS/iChatLoginTests/LoginStoreTests.swift
git commit -m "feat(ios): add LoginStore state machine with full unit tests"
```

---

## Task 6: PrimaryButton 组件

**Files:**
- Create: `UI/iOS/iChatLogin/Login/Components/PrimaryButton.swift`

(SwiftUI 视图较薄；UI 行为在 Task 12 E2E 中覆盖。)

- [ ] **Step 1: 实现 `PrimaryButton.swift`**

```swift
import SwiftUI

struct PrimaryButton: View {
    let title: String
    let isEnabled: Bool
    let action: () -> Void

    var body: some View {
        Button(action: action) {
            Text(title)
                .font(.bodyMd)
                .foregroundStyle(Color.bgSurface)
                .frame(maxWidth: .infinity, minHeight: 48)
                .background(isEnabled ? Color.brandPrimary : Color.brandPrimaryDisabled,
                            in: RoundedRectangle(cornerRadius: Shapes.buttonCornerRadius))
        }
        .disabled(!isEnabled)
    }
}
```

- [ ] **Step 2: 验证编译**

```bash
xcodebuild -scheme iChatLogin -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build | tail -5
```

Expected：BUILD SUCCEEDED。

- [ ] **Step 3: 提交**

```bash
git add UI/iOS/iChatLogin/Login/Components/PrimaryButton.swift
git commit -m "feat(ios): add PrimaryButton component"
```

---

## Task 7: PhoneField 组件

**Files:**
- Create: `UI/iOS/iChatLogin/Login/Components/PhoneField.swift`

- [ ] **Step 1: 实现 `PhoneField.swift`**

```swift
import SwiftUI

struct PhoneField: View {
    @Binding var rawDigits: String
    var onCommit: () -> Void = {}

    var body: some View {
        HStack(spacing: 12) {
            Text("+86").font(.bodyMd).foregroundStyle(Color.textSecondary)
            Rectangle().fill(Color.borderInput).frame(width: 1, height: 16)
            TextField(
                "请输入手机号",
                text: Binding(
                    get: { PhoneUtils.format(rawDigits) },
                    set: { rawDigits = String(PhoneUtils.digitsOnly($0).prefix(11)) }
                )
            )
            .keyboardType(.numberPad)
            .font(.bodyMd)
            .foregroundStyle(Color.textPrimary)
        }
        .padding(.horizontal, 16)
        .frame(height: 48)
        .background(Color.bgSurface, in: RoundedRectangle(cornerRadius: Shapes.inputCornerRadius))
        .overlay(
            RoundedRectangle(cornerRadius: Shapes.inputCornerRadius)
                .stroke(Color.borderInput, lineWidth: 1)
        )
    }
}
```

- [ ] **Step 2: 验证编译**

Expected：BUILD SUCCEEDED。

- [ ] **Step 3: 提交**

```bash
git add UI/iOS/iChatLogin/Login/Components/PhoneField.swift
git commit -m "feat(ios): add PhoneField component"
```

---

## Task 8: CodeBoxes 组件

**Files:**
- Create: `UI/iOS/iChatLogin/Login/Components/CodeBoxes.swift`

策略：用一个不可见的 `TextField` 接收键盘 + AutoFill，6 个 `Text` 框显示当前值的对应位。

- [ ] **Step 1: 实现 `CodeBoxes.swift`**

```swift
import SwiftUI

struct CodeBoxes: View {
    @Binding var value: String
    let isError: Bool
    @FocusState private var focused: Bool

    var body: some View {
        ZStack {
            HStack(spacing: 8) {
                ForEach(0..<6, id: \.self) { i in
                    let ch = (i < value.count) ? String(value[value.index(value.startIndex, offsetBy: i)]) : ""
                    let isFocus = (i == value.count) && !isError && focused
                    Text(ch)
                        .font(.codeBox)
                        .foregroundStyle(Color.textPrimary)
                        .frame(width: 48, height: 48)
                        .background(Color.bgSurface,
                                    in: RoundedRectangle(cornerRadius: Shapes.codeBoxCornerRadius))
                        .overlay(
                            RoundedRectangle(cornerRadius: Shapes.codeBoxCornerRadius)
                                .stroke(
                                    isError ? Color.stateError
                                            : (isFocus ? Color.borderInputFocus : Color.borderInput),
                                    lineWidth: 1
                                )
                        )
                }
            }
            // 不可见 TextField 接收输入与短信 AutoFill
            TextField("", text: Binding(
                get: { value },
                set: { value = String($0.filter(\.isWholeNumber).prefix(6)) }
            ))
            .keyboardType(.numberPad)
            .textContentType(.oneTimeCode)
            .focused($focused)
            .opacity(0.001)            // 视觉上不可见，但仍可获焦
            .accessibilityIdentifier("code-input")
        }
        .frame(height: 48)
        .onAppear { focused = true }
    }
}
```

- [ ] **Step 2: 验证编译**

Expected：BUILD SUCCEEDED。

- [ ] **Step 3: 提交**

```bash
git add UI/iOS/iChatLogin/Login/Components/CodeBoxes.swift
git commit -m "feat(ios): add CodeBoxes with auto-focus + iOS OTP AutoFill"
```

---

## Task 9: PhoneView

**Files:**
- Create: `UI/iOS/iChatLogin/Login/PhoneView.swift`

- [ ] **Step 1: 实现 `PhoneView.swift`**

```swift
import SwiftUI

struct PhoneView: View {
    @ObservedObject var store: LoginStore

    var body: some View {
        VStack(spacing: 20) {
            Spacer().frame(height: 24)

            // Logo 圆形
            Text("💬")
                .font(.system(size: 28))
                .frame(width: 56, height: 56)
                .background(Color.bgSurface, in: Circle())
                .shadow(color: Color.brandPrimary.opacity(0.18), radius: 12, y: 6)

            VStack(spacing: 4) {
                Text("欢迎使用 iChat").font(.titleLg).foregroundStyle(Color.textPrimary)
                Text("输入手机号，开启陪伴").font(.bodySm).foregroundStyle(Color.textSecondary)
            }

            PhoneField(rawDigits: Binding(
                get: { store.phone },
                set: { store.onPhoneChange($0) }
            ))
            .accessibilityIdentifier("phone-input")

            PrimaryButton(
                title: store.isSubmitting ? "发送中…" : "获取验证码",
                isEnabled: store.canRequestCode
            ) {
                Task { await store.requestCode() }
            }
            .accessibilityIdentifier("get-code-btn")

            if let err = store.errorMessage {
                Text(err).font(.caption).foregroundStyle(Color.stateError)
            }

            Spacer()

            Text("未注册手机号将自动创建账号")
                .font(.caption)
                .foregroundStyle(Color.textTertiary)
        }
        .padding(.horizontal, 24)
        .padding(.vertical, 32)
        .iChatPage()
    }
}
```

- [ ] **Step 2: 验证编译**

Expected：BUILD SUCCEEDED。

- [ ] **Step 3: 提交**

```bash
git add UI/iOS/iChatLogin/Login/PhoneView.swift
git commit -m "feat(ios): add PhoneView"
```

---

## Task 10: CodeView

**Files:**
- Create: `UI/iOS/iChatLogin/Login/CodeView.swift`

- [ ] **Step 1: 实现 `CodeView.swift`**

```swift
import SwiftUI

struct CodeView: View {
    @ObservedObject var store: LoginStore

    var body: some View {
        VStack(spacing: 16) {
            // 顶部返回栏
            HStack {
                Button { store.goBack() } label: {
                    Text("‹").font(.titleLg).foregroundStyle(Color.textPrimary)
                        .frame(width: 44, height: 44)
                }
                .accessibilityIdentifier("back-btn")
                Spacer()
            }
            .frame(height: 44)

            VStack(spacing: 4) {
                Text("输入验证码").font(.titleLg).foregroundStyle(Color.textPrimary)
                Text("已发送至 +86 \(PhoneUtils.mask(store.phone))")
                    .font(.bodySm).foregroundStyle(Color.textSecondary)
            }

            Spacer().frame(height: 8)

            CodeBoxes(
                value: Binding(
                    get: { store.code },
                    set: { v in Task { await store.onCodeChange(v) } }
                ),
                isError: store.errorMessage != nil
            )

            if let err = store.errorMessage {
                Text(err).font(.caption).foregroundStyle(Color.stateError)
            }

            if store.countdown > 0 {
                Text("\(store.countdown)s 后重新发送").font(.caption).foregroundStyle(Color.brandPrimary)
            } else {
                Button { Task { await store.resendCode() } } label: {
                    Text("重新发送").font(.caption).foregroundStyle(Color.brandPrimary)
                }
                .accessibilityIdentifier("resend-btn")
            }

            Spacer()

            Text("收不到验证码？").font(.caption).foregroundStyle(Color.brandPrimary)
        }
        .padding(.horizontal, 24)
        .iChatPage()
    }
}
```

- [ ] **Step 2: 验证编译**

Expected：BUILD SUCCEEDED。

- [ ] **Step 3: 提交**

```bash
git add UI/iOS/iChatLogin/Login/CodeView.swift
git commit -m "feat(ios): add CodeView"
```

---

## Task 11: 导航 + 顶层入口 + Home 占位

**Files:**
- Create: `UI/iOS/iChatLogin/Home/HomePlaceholderView.swift`
- Modify: `UI/iOS/iChatLogin/ContentView.swift`

- [ ] **Step 1: 创建 `HomePlaceholderView.swift`**

```swift
import SwiftUI

struct HomePlaceholderView: View {
    var body: some View {
        ZStack {
            Color.bgPage.ignoresSafeArea()
            Text("登录成功 · iChat").font(.titleLg).foregroundStyle(Color.textPrimary)
        }
        .accessibilityIdentifier("home-screen")
    }
}
```

- [ ] **Step 2: 修改 `ContentView.swift`**

```swift
import SwiftUI

struct ContentView: View {
    @StateObject private var store = LoginStore()

    var body: some View {
        if store.didLoginSuccess {
            HomePlaceholderView()
        } else {
            NavigationStack {
                PhoneView(store: store)
                    .navigationDestination(isPresented: Binding(
                        get: { store.phase == .code },
                        set: { isPresented in if !isPresented { store.goBack() } }
                    )) {
                        CodeView(store: store).navigationBarBackButtonHidden(true)
                    }
            }
        }
    }
}
```

- [ ] **Step 3: 验证编译**

Expected：BUILD SUCCEEDED。

- [ ] **Step 4: 模拟器手动 happy path**

在 Xcode ⌘R 启动 iPhone 15 模拟器，肉眼检查：
1. 进入 PhoneView，背景 `#F2F6FF`，按钮 disabled
2. 输入 `13800138000` → 按钮高亮
3. 点 "获取验证码" → 800ms 后切到 CodeView，倒计时 60 → 59 → ...
4. 输入 `123456` → 600ms 后切到 HomePlaceholderView 显示 "登录成功 · iChat"
5. 重启 → 输入手机号 → 进 CodeView → 输 `000000` → 红边 + 错误提示 → 1.2s 后清空回第一格

- [ ] **Step 5: 提交**

```bash
git add UI/iOS/iChatLogin/Home UI/iOS/iChatLogin/ContentView.swift
git commit -m "feat(ios): wire navigation + home placeholder"
```

---

## Task 12: UI 端到端测试（XCUITest）

**Files:**
- Create: `UI/iOS/iChatLoginUITests/LoginE2ETests.swift`

覆盖 spec §6.1 测试用例 #1, #3, #4, #5, #6。

- [ ] **Step 1: 写 E2E 测试**

```swift
import XCTest

final class LoginE2ETests: XCTestCase {

    override func setUpWithError() throws {
        continueAfterFailure = false
    }

    func test_happyPath_phoneToCodeToHome() {
        let app = XCUIApplication()
        app.launch()

        // 用例 #1：未满 11 位 → 按钮 disabled
        let getCodeBtn = app.buttons["get-code-btn"]
        XCTAssertTrue(getCodeBtn.exists)
        XCTAssertFalse(getCodeBtn.isEnabled)

        // 输入手机号
        let phoneField = app.textFields["phone-input"]
        phoneField.tap()
        phoneField.typeText("13800138000")
        XCTAssertTrue(getCodeBtn.isEnabled)

        // 用例 #3：发码 → 切到 Code 屏
        getCodeBtn.tap()
        XCTAssertTrue(app.staticTexts["输入验证码"].waitForExistence(timeout: 2.0))
        XCTAssertTrue(app.staticTexts["已发送至 +86 138****8000"].exists)

        // 用例 #5：输入正确验证码 → 跳主页
        let codeInput = app.textFields["code-input"]
        codeInput.tap()
        codeInput.typeText("123456")
        XCTAssertTrue(app.otherElements["home-screen"].waitForExistence(timeout: 2.0))
        XCTAssertTrue(app.staticTexts["登录成功 · iChat"].exists)
    }

    func test_backFromCode_keepsPhone() {
        let app = XCUIApplication()
        app.launch()

        let phoneField = app.textFields["phone-input"]
        phoneField.tap(); phoneField.typeText("13800138000")
        app.buttons["get-code-btn"].tap()
        XCTAssertTrue(app.staticTexts["输入验证码"].waitForExistence(timeout: 2.0))

        app.buttons["back-btn"].tap()
        XCTAssertTrue(app.buttons["get-code-btn"].waitForExistence(timeout: 1.0))
        XCTAssertTrue(app.buttons["get-code-btn"].isEnabled)  // 用例 #6：手机号保留
    }

    func test_wrongCode_showsErrorThenClears() {
        let app = XCUIApplication()
        app.launch()

        app.textFields["phone-input"].tap()
        app.textFields["phone-input"].typeText("13800138000")
        app.buttons["get-code-btn"].tap()
        XCTAssertTrue(app.staticTexts["输入验证码"].waitForExistence(timeout: 2.0))

        let codeInput = app.textFields["code-input"]
        codeInput.tap(); codeInput.typeText("000000")

        XCTAssertTrue(app.staticTexts["验证码错误，请重新输入"].waitForExistence(timeout: 2.0))
        // 1.2s 后自动清空
        let cleared = NSPredicate(format: "exists == FALSE")
        expectation(for: cleared, evaluatedWith: app.staticTexts["验证码错误，请重新输入"])
        waitForExpectations(timeout: 2.0)
    }
}
```

- [ ] **Step 2: 跑 E2E**

```bash
cd UI/iOS && xcodebuild test \
  -scheme iChatLogin \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:iChatLoginUITests/LoginE2ETests | tail -30
```

Expected：3 tests passed。

- [ ] **Step 3: 提交**

```bash
git add UI/iOS/iChatLoginUITests/LoginE2ETests.swift
git commit -m "test(ios): add XCUITest E2E (happy path + back + wrong code)"
```

---

## Task 13: 跑完所有测试 + 视觉走查

- [ ] **Step 1: 跑全部单元测试**

```bash
cd UI/iOS && xcodebuild test \
  -scheme iChatLogin \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:iChatLoginTests | tail -10
```

Expected：20 tests passed（PhoneUtilsTests 7 + MockAuthServiceTests 3 + LoginStoreTests 10）。

- [ ] **Step 2: 跑全部 UI 测试**

```bash
xcodebuild test -scheme iChatLogin -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:iChatLoginUITests | tail -10
```

Expected：3 tests passed。

- [ ] **Step 3: 视觉走查**

启动 App，对照 `.superpowers/brainstorm/final-design.html` 移动端三张图：

1. **PhoneView**：背景 `#F2F6FF`、Logo 圆形白底带阴影、标题 "欢迎使用 iChat"、副标题 "输入手机号，开启陪伴"、输入框圆角 14、按钮 pill 圆角 24、底部 "未注册手机号将自动创建账号"
2. **CodeView 等待态**：左上返回 ‹、6 个验证码格子（首格高亮蓝边）、副标题脱敏手机号、"60s 后重新发送"
3. **CodeView 错误态**：6 格全部红边、下方 "验证码错误，请重新输入"

任意视觉差异回到 `Theme/*` 调整后重新跑测试再次走查。

- [ ] **Step 4: 最终提交（如有视觉调整）**

```bash
git add -A && git commit -m "chore(ios): visual walkthrough adjustments"
```

否则跳过此步。

---

## Done When

- [ ] 所有单元测试通过（20 个）
- [ ] 所有 UI 测试通过（3 个）
- [ ] 模拟器手动走 happy path：启动 → 输 `13800138000` → 获取验证码 → 输 `123456` → 进入"登录成功"占位
- [ ] 视觉与 spec §4 token 一致
- [ ] 性能：从 launcher 点击图标到 PhoneView 可交互 ≤ 2s

---

## 已知不在第一期范围（Out of Scope，参见 spec §1.3）

- "收不到验证码？" 链接为静态文字，无点击行为
- 真实后端联调
- 短信 AutoFill 真实联调（仅声明 `textContentType = .oneTimeCode` 留好 hook）
- 暗色模式
- 横屏 / iPad 布局
- 启动隐私协议弹窗
